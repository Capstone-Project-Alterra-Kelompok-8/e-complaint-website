export default function ButtonSimpanNews({ onClick }) {
    return (
        <button className="btn bg-green-600 w-24 rounded-sm text-white" onClick={onClick}>
            Simpan
        </button>
    );
}